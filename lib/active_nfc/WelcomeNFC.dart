import 'package:flutter/material.dart';

import 'active_nfc.dart';

class WelcomeNFC extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
